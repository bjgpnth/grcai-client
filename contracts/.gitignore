# Contract files are in grcai-contracts; do not commit them here.
# README.md and version.txt are tracked (version.txt is required at runtime for
# X-GRCAI-Contracts-Version / Docker COPY contracts/).
*
!.gitignore
!README.md
!version.txt
